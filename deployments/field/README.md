# Field deployment evidence

Real domain-center inventories, rendered bundles, runtime configuration,
certificates, tokens, database backups, and raw evidence belong under:

```text
deployments/field/private/
```

That directory is ignored by Git. Only reviewed, redacted summaries may be
committed elsewhere. Do not move private artifacts into this README directory.
