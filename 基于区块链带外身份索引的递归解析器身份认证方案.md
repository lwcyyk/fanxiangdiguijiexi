# 基于区块链带外身份索引的递归解析器身份认证方案

## 一、方案概述

本项目面向 DNS 解析过程中递归解析器身份难以确认的问题，在保留现有 DNS 查询体系的基础上，增加一条独立的带外身份认证链路。

系统中始终存在两条链路：

```text
第一条：正常 DNS 查询链路
负责完成域名到 IP 地址的解析

第二条：带外身份认证链路
负责验证参与解析的递归或转发解析器身份
```

正常 DNS 链路继续按照原有方式运行，不修改 DNS 协议、报文格式、递归机制、权威服务器体系和底层网络设备。身份认证功能全部部署在应用层，通过本地解析封装器、解析器身份 Agent、链下身份索引和区块链可信锚完成。

系统的基本工作方式如下：

```text
应用发起 DNS 查询
        ↓
现有 DNS 链路完成域名解析
        ↓
本地封装器暂存 DNS 结果
        ↓
带外链路验证相关递归解析器身份
        ↓
全部验证通过
→ 向应用返回解析结果

任意身份验证失败
→ 丢弃结果
→ 切换备用解析器或返回 SERVFAIL
```

本方案的核心思想是：通过一条独立于正常 DNS 解析路径的带外通道，为客户端提供递归解析器身份的发现和认证，避免使用被验证解析器本身提供其认证材料。

---

## 二、建设目标与设计原则

### 2.1 建设目标

本项目主要解决以下问题：

1. 客户端通常只获得递归解析器的 IP、名称或 URI，无法确认解析器真实身份。
2. DHCP、IPv6 RA、VPN 或人工配置可能向客户端提供错误或恶意解析器地址。
3. 递归解析链中可能存在多级转发器或上游解析器，仅凭来源 IP 无法判断其运营者和合法状态。
4. 解析器身份可能发生更新、密钥轮换、端点迁移或撤销。
5. 每次 DNS 查询都访问区块链会带来较大延迟，需要通过本地缓存控制开销。
6. 整套机制需要在应用层完成，不能破坏现有 DNS 兼容性。

最终目标是：

> 让本地验证器能够独立确认参与 DNS 解析的递归或转发解析器是谁、由谁运营、使用哪些服务端点、当前是否有效，并根据认证结果决定是否接受 DNS 解析结果。

### 2.2 设计原则

#### 保留现有 DNS 体系

系统不修改：

- DNS 查询和响应格式；
- UDP/TCP DNS 协议；
- DoH、DoT、DoQ 标准；
- 根、顶级域和权威服务器体系；
- 操作系统网络栈；
- 路由器、交换机和网卡；
- iptables、eBPF 或内核转发机制。

#### 身份链路独立

解析器身份信息不能通过正在被验证的解析器查询。

例如，要验证解析器 R1 时，不能让 R1 替客户端解析区块链 RPC 或身份 Indexer 的地址。带外链路应使用：

- 本地同步数据；
- 预配置 IP；
- 固定 HTTPS 端点；
- 独立管理网络；
- 本地区块链节点；
- 证书固定或公钥固定。

#### 身份与地址分离

IP 只是解析器的服务端点，不是解析器的稳定身份。

系统使用：

```text
resolver-id
```

唯一标识解析器实例或集群，再将一个或多个 IP、名称、端口和协议绑定到该身份。

#### 链下保存完整对象，链上保存可信锚

链下数据库负责快速查询，区块链负责公开、可审计的对象指纹和状态。

#### 冷路径完整认证，热路径使用缓存

首次发现、缓存过期、对象更新或撤销时执行完整带外认证。正常运行阶段优先读取本地可信缓存。

---

## 三、总体系统架构

### 3.1 正常 DNS 查询链路

正常查询链路负责域名解析：

```text
应用程序
    ↓
本地 DNS 验证封装器
    ↓
第一跳递归解析器 R1
    ↓
上游转发或递归解析器 R2
    ↓
其他 DNS 服务器或权威服务器
    ↓
返回域名对应 IP
```

这一链路只负责产生 DNS 结果，不负责证明解析器身份。

### 3.2 带外身份认证链路

身份认证链路独立运行：

```text
本地 DNS 验证封装器
    ↓
本地可信缓存
    ↓ 缓存未命中或失效
链下身份 Indexer
    ↓
区块链 ResolverIdentityRegistry
```

该链路回答：

```text
这个解析器是谁？
由谁运营？
这个 IP、名称和服务端点是否属于它？
身份对象是否被篡改？
它现在是否有效或已被撤销？
```

### 3.3 主要组成部分

#### 1. 本地 DNS 验证封装器

部署在客户端应用层，向应用提供本地 DNS 接口，例如：

```text
127.0.0.1:53
```

主要职责：

- 接收应用 DNS 查询；
- 调用现有递归解析器；
- 暂存 DNS 结果；
- 获取需要验证的解析器列表；
- 调用身份认证模块；
- 根据验证结果释放或拒绝 DNS 响应；
- 维护备用解析器和失败重试；
- 提供日志、Proof API 和监控指标。

#### 2. 解析器身份 Agent

部署在参与系统的递归或转发解析器旁边，以 Sidecar 或独立进程运行。

主要职责：

- 声明本机的 `resolver-id`；
- 提供本机端点和运营者信息；
- 记录或暴露当前配置的上游解析器；
- 向应用层验证器提供解析链观测信息；
- 监听身份更新和撤销状态；
- 不修改 DNS 协议和 DNS 报文。

#### 3. 链下身份 Indexer

保存完整的解析器身份对象，初期可使用 SQLite。

主要职责：

- 按 IP、名称和 `resolver-id` 查询；
- 返回完整身份对象；
- 返回对象签名；
- 返回 object hash；
- 返回 Merkle proof 和 state root；
- 保存版本和撤销记录；
- 提供审计日志。

#### 4. 区块链身份注册表

智能合约负责保存轻量可信状态：

```text
resolver-id → object hash
endpoint key → resolver-id
state root → root status
resolver-id → ACTIVE / REVOKED / SUSPENDED
```

区块链不是 DNS 查询数据库，也不保存完整身份 JSON。

#### 5. 本地可信缓存

本地缓存保存已经验证的解析器身份对象及验证证据，避免每次 DNS 查询都访问带外系统。

#### 6. 身份发布与管理工具

供解析器运营者使用，负责：

- 创建 `resolver-id`；
- 登记运营者；
- 登记解析器端点；
- 生成 canonical JSON；
- 计算 object hash；
- 签名身份对象；
- 发布链上锚定；
- 更新或撤销解析器身份。

---

## 四、解析器身份对象设计

### 4.1 真实性对象

核心数据对象定义为：

```text
ResolverAuthenticityObjectV1
```

建议结构如下：

```json
{
  "schema_version": "resolver-auth-object-v1",
  "canonical_version": 1,

  "resolver_id": "operator-a/resolver-cluster-01",
  "resolver_type": "recursive-forwarder",

  "operator": {
    "operator_id": "operator-a",
    "name": "Operator A",
    "trust_domain": "operator-a.example"
  },

  "endpoints": [
    {
      "endpoint_id": "endpoint-01",
      "ip": "192.0.2.53",
      "port": 853,
      "transport": "dot",
      "server_name": "dns.operator-a.example",
      "alpn": ["dot"]
    },
    {
      "endpoint_id": "endpoint-02",
      "uri": "https://dns.operator-a.example/dns-query",
      "transport": "doh",
      "server_name": "dns.operator-a.example",
      "alpn": ["h2", "h3"]
    }
  ],

  "attestation": {
    "type": "operator-signature",
    "value": "..."
  },

  "valid_from": "2026-07-01T00:00:00Z",
  "valid_until": "2027-07-01T00:00:00Z",
  "status": "ACTIVE",

  "object_version": 1,
  "issuer": "resolver-trust-authority-01",
  "key_id": "issuer-key-01",
  "signature": "base64:..."
}
```

### 4.2 resolver-id

`resolver-id` 用于稳定标识一个解析器实例或集群。

建议格式：

```text
<operator-id>/<resolver-instance-or-cluster-id>
```

例如：

```text
operator-a/tokyo-cluster-01
university-x/campus-resolver-02
isp-b/public-recursive-east
```

IP 发生变化时，可以继续使用原来的 `resolver-id`，只更新端点列表和对象版本。

### 4.3 端点绑定

一个解析器身份可以绑定多个端点：

```text
resolver-id
├── IPv4 + UDP/TCP 53
├── IPv4 + DoT 853
├── DoH URI
├── DoQ 端点
└── 备用解析节点
```

验证器不能只检查 IP，还应检查：

- IP 或 URI；
- 端口；
- 传输协议；
- TLS 服务器名称；
- ALPN；
- 对象版本；
- 有效期。

### 4.4 查询键

支持三种发现方式。

#### 按 IP 发现

\[
K_{ip}=SHA256(\text{"ip:"}\parallel CanonicalIP)
\]

#### 按名称发现

\[
K_{name}=SHA256(\text{"name:"}\parallel NormalizedName)
\]

#### 按 resolver-id 发现

\[
K_{id}=SHA256(\text{"resolver-id:"}\parallel NormalizedResolverID)
\]

索引关系为：

```text
IP lookup key
→ resolver-id key

Name lookup key
→ resolver-id key

resolver-id key
→ object hash
```

### 4.5 canonical JSON 和 object hash

发布和验证时需要使用完全一致的规范化规则。

处理过程：

```text
移除 signature
→ 固定字段顺序
→ 规范域名、IP 和时间
→ 生成 canonical JSON
→ SHA-256
```

公式为：

\[
H_{\text{object}}
=
SHA256(Canonical(ResolverAuthenticityObject))
\]

本地重新计算的 hash 必须等于链上保存的 hash：

\[
H_{\text{local}}=H_{\text{on-chain}}
\]

---

## 五、链上链下存储设计

### 5.1 链下 SQLite

SQLite 主要保存：

```text
完整解析器身份对象
运营者信息
端点列表
对象签名
object hash
对象版本
有效期
撤销状态
IP 和名称查询索引
Merkle proof
state root
审计日志
```

建议表结构：

```text
resolver_objects
resolver_endpoints
resolver_lookup_index
resolver_revocations
state_roots
audit_logs
```

SQLite 适用于当前研究原型和单机部署。生产环境可以平滑迁移到 PostgreSQL，不改变整体信任模型。

### 5.2 区块链合约

合约建议命名：

```text
ResolverIdentityRegistryV1
```

核心数据结构可以为：

```solidity
struct ResolverAnchor {
    bytes32 resolverIdKey;
    bytes32 objectHash;
    bytes32 stateRoot;
    uint64 objectVersion;
    uint64 validUntil;
    uint8 status;
}
```

主要映射：

```text
endpointKey → resolverIdKey
resolverIdKey → ResolverAnchor
stateRoot → RootStatus
```

### 5.3 合约状态

解析器状态包括：

```text
ACTIVE
SUSPENDED
REVOKED
EXPIRED
UNKNOWN
```

只有 `ACTIVE` 状态允许通过验证。

### 5.4 链上事件

合约应产生以下事件：

```text
ResolverPublished
ResolverUpdated
ResolverRevoked
EndpointBound
EndpointUnbound
RootPublished
RootRevoked
```

本地应用层监听器收到事件后，及时失效相关缓存。

---

## 六、完整运行流程

### 6.1 身份发布阶段

解析器投入使用前，运营者需要完成身份登记。

流程如下：

```text
1. 创建稳定 resolver-id
2. 登记运营者信息
3. 登记解析器 IP、名称、端口和协议
4. 设置有效期和对象版本
5. 生成 canonical JSON
6. 计算 object hash
7. 使用运营者或信任机构私钥签名
8. 将完整对象写入链下 Indexer
9. 构造 Merkle tree 和 state root
10. 将 object hash 和 state root 发布到区块链
11. 将解析器状态设置为 ACTIVE
```

### 6.2 正常 DNS 查询阶段

应用发起查询：

```text
example.com A
```

本地封装器将查询转给现有递归解析器。

正常 DNS 链路按照原有方式运行，并返回：

```text
example.com → 93.184.216.34
```

本地封装器收到结果后先放入待决区：

```text
PENDING_VERIFICATION
```

此时结果尚未交付应用。

### 6.3 确定需要验证的解析器

第一跳解析器可以通过实际连接信息直接获得：

```text
IP
端口
协议
名称或 URI
```

对于受控环境中的中间转发或上游解析器，可通过以下应用层方式获得：

- 本地已知的上游配置；
- 解析器身份 Agent；
- Sidecar 提供的当前转发关系；
- 应用层审计日志；
- 集中式解析链管理配置。

这里不修改 DNS 报文，只在应用层提供解析器列表或链路观测信息。

### 6.4 优先查询本地缓存

对每个需要验证的解析器：

```text
解析器 IP、名称或 resolver-id
        ↓
查询内存缓存
        ↓ 未命中
查询本地 SQLite 缓存
        ↓ 未命中或过期
访问带外身份 Indexer 和区块链
```

### 6.5 完整身份验证

缓存未命中时执行三段式验证。

#### 第一段：索引定位可信

检查：

```text
解析器 IP 或名称
→ endpoint key
→ resolver-id
→ object hash
```

验证：

- 查询键生成是否正确；
- 链上是否存在映射；
- 映射状态是否有效；
- 输入端点是否属于该 resolver-id。

#### 第二段：对象内容可信

从 Indexer 取得完整对象，验证：

```text
对象签名
issuer
key_id
canonical version
object hash
Merkle proof
state root
链上 object hash
链上 root 状态
```

#### 第三段：对象语义可用

检查：

```text
mandatory 字段是否可识别
对象是否已生效
对象是否过期
状态是否 ACTIVE
是否存在撤销
endpoint 是否匹配
传输协议是否符合本地策略
```

### 6.6 最终决策

假设本次需要验证的解析器集合为：

\[
R=\{R_1,R_2,\ldots,R_n\}
\]

只有满足：

\[
\bigwedge_{i=1}^{n} Verify(R_i)=true
\]

系统才接受 DNS 结果：

\[
Accept=
DNSResponseAvailable
\land
AllObservedResolversVerified
\]

如果任意解析器失败：

```text
拒绝当前 DNS 结果
→ 尝试备用第一跳或备用解析路径
→ 无可信路径时返回 SERVFAIL
```

---

## 七、本地可信缓存与性能优化

### 7.1 三层缓存

系统采用：

```text
一级：内存缓存
二级：本地 SQLite 持久缓存
三级：Indexer 和区块链权威状态
```

### 7.2 缓存内容

不能只存：

```text
192.0.2.53 → TRUSTED
```

应保存：

```json
{
  "resolver_id": "operator-a/resolver-cluster-01",
  "operator_id": "operator-a",

  "endpoint": {
    "ip": "192.0.2.53",
    "port": 853,
    "transport": "dot",
    "server_name": "dns.operator-a.example"
  },

  "object_hash": "0x...",
  "object_version": 3,
  "state_root": "0x...",

  "verified_at": "2026-07-07T10:00:00Z",
  "soft_expire_at": "2026-07-07T10:10:00Z",
  "hard_expire_at": "2026-07-07T11:00:00Z",

  "status": "VERIFIED"
}
```

### 7.3 缓存状态机

```text
UNKNOWN
   ↓ 完整验证成功
VERIFIED
   ↓ soft TTL 到期
REFRESHING
   ↓ 刷新成功
VERIFIED

VERIFIED
   ↓ hard TTL 到期
EXPIRED

VERIFIED
   ↓ 收到撤销事件
REVOKED

任意状态
   ↓ 验证失败
REJECTED
```

允许放行的状态：

```text
VERIFIED
REFRESHING 且未超过 hard TTL
```

### 7.4 soft TTL 和 hard TTL

#### soft TTL

到达后允许使用当前缓存，同时后台刷新。

#### hard TTL

超过后必须重新访问带外身份索引，重新认证成功前不能继续信任。

hard TTL 不得超过真实性对象的 `valid_until`。

### 7.5 撤销即时失效

本地 Agent 监听区块链事件。

收到：

```text
ResolverRevoked
EndpointUnbound
RootRevoked
```

后立即执行：

```text
删除内存缓存
将 SQLite 缓存标记为 REVOKED
停止接受该解析器返回的新结果
关闭相关持久连接
切换到备用解析器
```

### 7.6 并行认证

如果一次查询涉及多个可观测解析器，可以并行验证：

```text
Verify R1
Verify R2
Verify R3
```

而不是串行等待，降低冷缓存情况下的总延迟。

### 7.7 热路径

缓存命中时，身份验证只需要：

```text
本地缓存查找
+ 有效期检查
+ endpoint 匹配
+ 状态检查
```

不需要访问区块链。

因此性能特征为：

```text
第一次或状态变化：
完整带外认证，开销较大

稳定运行阶段：
本地缓存认证，开销较小
```

---

## 八、安全与可信边界

### 8.1 能够防御的风险

#### DHCP 或 RA 注入恶意解析器

客户端得到的解析器 IP 仍需要通过带外身份验证。

#### 解析器 IP 冒充

攻击者只控制相同 IP，无法伪造合法身份对象、签名和链上 hash。

#### Indexer 篡改数据

链下对象被修改后，其 hash 与链上 object hash 不一致。

#### 历史对象回放

旧对象即使签名正确，也会因为 object version、当前链上 hash 或状态不一致而失败。

#### IP 重新分配

缓存绑定 `resolver-id + 完整 endpoint + object version`，不会只凭旧 IP 继续信任。

#### 解析器撤销

链上撤销事件触发本地缓存即时失效。

#### 带外索引攻击

可通过以下机制降低影响：

- 本地缓存；
- 多个 Indexer 副本；
- 多个 RPC 端点；
- 请求限流；
- 后台刷新；
- 失败关闭策略。

### 8.2 fail-closed 规则

以下情况必须拒绝：

```text
身份对象不存在
签名错误
object hash 不一致
state root 无效
状态不是 ACTIVE
对象过期
endpoint 不匹配
对象版本过旧
缓存超过 hard TTL
撤销状态不明确
mandatory 字段无法理解
```

### 8.3 不能解决的问题

本方案不能单独保证：

- 注册时提交的信息一定真实；
- 信任机构不会错误签发；
- 区块链管理员密钥永不泄露；
- 完全不配合的中间解析器能够被客户端观测；
- 权威服务器本身已经完成身份认证；
- 恶意但具有合法身份的解析器一定返回正确 DNS 结果。

因此还需要：

- 运营者审核；
- 域和组织治理；
- 多签权限；
- 密钥轮换；
- 安全审计；
- 备用解析器；
- 与 DNSSEC 或其他结果完整性机制协同。

---

## 九、应用层实现方式

### 9.1 客户端侧

部署：

```text
Local Verification Wrapper
```

提供：

- 本地 UDP/TCP DNS 接口；
- 可选本地 DoH 接口；
- DNS 结果暂存；
- 身份认证调度；
- 缓存管理；
- 结果放行与拒绝。

应用只需将 DNS 服务器配置为本地封装器。

### 9.2 解析器侧

部署：

```text
Resolver Identity Agent
```

Agent 与现有解析器通过以下方式交换信息：

- 配置文件；
- 控制接口；
- 日志；
- 本地 Unix Socket；
- 管理 API。

该方式不修改 DNS 转发逻辑。

### 9.3 带外服务侧

部署：

```text
Resolver Indexer
ResolverIdentityRegistry
Event Watcher
Admin Publisher
```

各组件通过 HTTPS、mTLS 或本地 RPC 连接。

### 9.4 接口建议

#### Indexer 查询接口

```text
GET /v1/lookup/ip/{ip}
GET /v1/lookup/name/{name}
GET /v1/resolvers/{resolver_id}
GET /v1/resolvers/{resolver_id}/proof
GET /v1/resolvers/{resolver_id}/status
GET /v1/roots/{state_root}
```

#### 本地验证接口

```text
POST /v1/verify/resolver
POST /v1/verify/query
GET  /v1/cache
GET  /v1/proofs/{request_id}
GET  /healthz
GET  /metrics
```

#### 管理接口

```text
POST /v1/admin/operators
POST /v1/admin/resolvers
POST /v1/admin/resolvers/{id}/endpoints
POST /v1/admin/resolvers/{id}/revoke
POST /v1/admin/roots/publish
```

---

## 十、实施路线

### 第一阶段：单解析器身份认证闭环

完成：

```text
客户端
→ 本地验证封装器
→ 第一跳递归解析器
```

实现：

- ResolverAuthenticityObject；
- SQLite Indexer；
- object hash；
- 链上锚定；
- 本地缓存；
- 结果暂存与放行；
- 认证失败返回 SERVFAIL。

### 第二阶段：解析器身份 Agent

在解析器旁部署应用层 Agent，完成：

- resolver-id 配置；
- 端点发布；
- 上游配置读取；
- 身份状态查询；
- 应用层链路观测。

### 第三阶段：受控多级解析环境

搭建：

```text
客户端
→ R1
→ R2
→ R3
```

验证所有可观测的递归和转发解析器身份。

### 第四阶段：缓存与撤销

实现：

- 内存缓存；
- SQLite 持久缓存；
- soft TTL；
- hard TTL；
- 区块链事件监听；
- 缓存即时失效；
- 多 RPC 和多 Indexer 容错。

### 第五阶段：性能和安全实验

完成大规模并发、冷缓存、热缓存、撤销传播和攻击实验。

### 第六阶段：RFC 9499 联动

将 ODAR 对象中的 `resolver-id` 与 RFC 9499 中的 Recursive-Identifier 统一，用于权威服务器识别递归解析器。

该部分属于后续扩展，不影响当前双链路架构。

---

## 十一、实验方案

### 11.1 功能闭环

验证：

```text
正常 DNS 查询
→ DNS 结果暂存
→ 解析器身份认证
→ 验证通过后释放结果
```

### 11.2 恶意解析器注入

通过 DHCP 或配置文件替换递归解析器 IP，检查系统是否拒绝未登记或身份不匹配的解析器。

### 11.3 链下数据篡改

修改 SQLite 中的：

- resolver-id；
- operator；
- endpoint；
- valid_until；
- object version。

预期 object hash 验证失败。

### 11.4 历史对象回放

返回旧版本但签名正确的身份对象，预期因链上当前 hash 或版本不同而拒绝。

### 11.5 冷缓存与热缓存性能

分别测试：

```text
冷缓存：
Indexer + 区块链 + 签名 + hash + proof 验证

热缓存：
本地缓存查询 + 有效期和状态检查
```

采集：

- 平均时延；
- P95；
- P99；
- 缓存命中率；
- 带外查询次数；
- DNS 总时延。

### 11.6 多解析器并行验证

测试涉及 1 个、2 个、3 个解析器时：

- 串行验证延迟；
- 并行验证延迟；
- 冷缓存开销；
- 热缓存开销。

### 11.7 撤销传播

将某解析器状态修改为 `REVOKED`，测量：

```text
链上撤销
→ Agent 收到事件
→ 本地缓存失效
→ 新 DNS 结果被拒绝
```

### 11.8 带外通道故障

模拟：

- Indexer 不可用；
- 区块链 RPC 不可用；
- 缓存仍有效；
- 缓存已经过期。

检查系统是否按照降级和 fail-closed 策略工作。

---

## 十二、项目创新点

### 1. 双链路架构

正常 DNS 链路负责解析，带外身份链路负责认证，两者相互独立。

### 2. 不修改 DNS 基础设施

所有功能在应用层完成，不修改 DNS 协议、网络层或硬件设备。

### 3. 解析器地址到身份的可信绑定

把 IP、名称和 URI 统一映射到稳定的 `resolver-id` 及运营者身份。

### 4. 链上链下协同

链下保存完整身份对象，链上保存 object hash、版本和状态，兼顾查询效率与防篡改能力。

### 5. 结果延迟交付

DNS 结果先暂存，身份认证通过后再交给应用，防止应用提前使用不可信响应。

### 6. 分层可信缓存

首次验证访问带外系统，后续查询优先使用本地缓存，降低区块链查询对 DNS 时延的影响。

### 7. 身份撤销实时传播

通过链上事件及时失效本地缓存，避免继续使用已撤销解析器。

### 8. 支持受控多级递归环境

在解析器 Agent 提供链路观测信息的情况下，可以认证解析过程中涉及的多级递归和转发解析器。

---

## 十三、最终方案表述

本项目在应用层构建正常 DNS 查询链路和带外递归解析器身份认证链路。正常 DNS 链路保持现有协议和递归解析方式不变，负责完成域名到 IP 地址的解析。DNS 响应返回后，本地验证封装器暂存结果，并通过独立带外通道验证本次查询中能够观测到的递归和转发解析器身份。

解析器运营者提前发布包含 `resolver-id`、运营者、服务端点、连接参数、有效期和签名的真实性对象，完整对象保存在链下 Indexer 中，其 object hash、对象版本、state root 和撤销状态锚定在区块链上。

验证器首先查询本地可信缓存，缓存未命中、过期、对象更新或撤销时，再访问带外 Indexer 和区块链，验证索引关系、对象签名、内容完整性、端点绑定、有效期和状态。

所有相关解析器身份均通过验证后，原始 DNS 响应才交付应用；任意解析器身份验证失败时，系统丢弃当前结果，并选择备用解析器或返回 `SERVFAIL`。整个机制通过本地封装器和解析器身份 Agent 部署在应用层，不修改现有 DNS 协议、报文格式、网络层和硬件架构。

最终核心流程为：

```text
应用发起 DNS 查询
        ↓
现有 DNS 链路完成域名解析
        ↓
本地应用层封装器暂存 DNS 结果
        ↓
确定可观测的递归/转发解析器
        ↓
查询本地可信缓存
        ↓
必要时访问带外 Indexer 和区块链
        ↓
验证 resolver-id、运营者、端点、签名和状态
        ↓
全部通过
→ 向应用返回 DNS 结果

任意失败
→ 丢弃结果
→ 切换备用解析器或返回 SERVFAIL
```

本方案的最终定位是：

> 在不改变现有 DNS 架构的前提下，通过应用层带外身份索引，对 DNS 解析过程中涉及的递归解析器进行独立身份认证，并以认证结果控制 DNS 响应是否能够被应用使用。
